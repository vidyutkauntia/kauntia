Drop an EV-bus background video here named exactly:  hero-ev.mp4

Recommended: 1920x1080 (or 1280x720), H.264 MP4, muted, 10-20s seamless loop, under ~5 MB.
It auto-plays muted and loops behind the hero at low opacity with a contrast overlay,
so headline text stays fully readable. If this file is absent, the animated
gradient background shows instead (graceful fallback).
